<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.onestraw.poovali">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/watering_can"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="org.onestraw.poovali.PlantListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.onestraw.poovali.PlantDetailActivity"
            android:label="@string/title_activity_plant_detail"
            android:parentActivityName="org.onestraw.poovali.PlantListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.onestraw.poovali.PlantListActivity" />
        </activity>
    </application>

</manifest>