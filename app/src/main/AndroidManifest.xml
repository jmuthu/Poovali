<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.onestraw.poovali">

    <uses-permission android:name="android.alarm.permission.SET_ALARM" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/watering_can"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:launchMode="singleTop">
        <activity android:name=".activity.PlantListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.PlantDetailActivity"
            android:label="@string/title_activity_plant_detail"
            android:parentActivityName=".activity.PlantListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.onestraw.poovali.activity.PlantListActivity" />
        </activity>
        <activity
            android:name=".activity.AddEventActivity"
            android:label="@string/title_activity_add_event"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity
            android:name=".activity.ViewEventActivity"
            android:parentActivityName=".activity.PlantListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.onestraw.poovali.activity.PlantListActivity" />
        </activity>

        <receiver android:name=".AlarmReceiver" />
    </application>

</manifest>